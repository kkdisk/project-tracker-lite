{
  "name": "project-tracker",
  "version": "1.0.0",
  "description": "Project Tracker - Task Management System with Gantt Chart",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch",
    "dev": "python -m http.server 8000"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}